{
  "name": "purescript-snabbdom",
  "version": "0.1.0",
  "license": "SPDX(MIT)",
  "author": "Zach Smith zach_asmith@yahoo.com https://aryzach.github.io",
  "description": "A Purescript wrapper the Snabbdom virtual DOM library",
  "files": [
    "package.json",
    "index.js"
  ],
  "scripts": {
    "live": "pulp --watch --before 'clear' build",
    "serve": "http-server",
    "setup": "npm install && bower install",
    "clean": "rm -rf bower_components && rm -rf node_modules && rm -rf output",
    "browse": "pulp --watch browserify --to index.js",
    "dev": "pulp --watch browserify --to index.js && http-server"
  },
  "dependencies": {
    "snabbdom": "^0.7.1"
  },
  "devDependencies": {
    "pulp": "^12.3.0"
  }
}
